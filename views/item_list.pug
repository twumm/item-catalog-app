extends layout

block content
  h1= title

  ul
  each item in item_list
    li
      a(href=item.url) #{item.title}
      if item.category
        |  (#{item.category.title})
      else
        |  (No category title)

  else 
    li There are no items.